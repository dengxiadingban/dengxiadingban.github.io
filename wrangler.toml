name = "business-consulting-payment"
main = "worker.js"
compatibility_date = "2024-01-01"

# 环境变量配置（在Cloudflare Dashboard中设置）
# 不要在这里直接填写真实的密钥值！
[vars]
# EPAY_PID_ENV = "your_pid"
# EPAY_KEY_ENV = "your_key"
# EPAY_API_URL_ENV = "https://pay.example.com/submit.php"

# 这些变量需要在Cloudflare Dashboard的Workers设置中配置
# Settings -> Variables -> Environment Variables

